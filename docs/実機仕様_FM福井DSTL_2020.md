# 実機仕様 — MN バンド D-STL（FM 福井向け, RFデザイン 2020）

出典: **納入仕様書「DST/DSR-6500SR MN バンドデジタル STL 送受信装置」**（RF デザイン株式会社、
日本エレクトロニクスシステムズ殿／エフエム福井殿向け、2020/6/23）。ユーザー提供 PDF から抽出。
これが本プロジェクトの**実機基準値**（従来設計）。最終更新: 2026-09-03

---

## 1. 送信 RF（実測基準）

| 項目 | 値 |
|---|---|
| **送信出力** | **1 W（+30 dBm）** |
| 送信周波数 | MAF-33 = **6716.625 MHz**（M バンド 6570–6870 MHz 内） |
| 周波数安定度 | 外部 10 MHz 同期（±2 Hz）、フリーラン ±1×10⁻⁷ |
| **占有帯域幅** | **≤ 405 kHz** |
| **スペクトルマスク** | **−37 dBc 以下（fo±250 kHz）／ −48 dBc 以下（fo±750 kHz）** |
| スプリアス | スプリアス領域 ≤50 µW／帯域外領域 ≤100 µW |
| モニタ出力 | RF −20 dBm typ.、TX-LO 6586.625 MHz、CLK 100 MHz(0 dBm) |

## 2. 64QAM 変調部（実機パラメータ）★重要

| 項目 | 値 |
|---|---|
| 変調方式 | **64QAM** |
| **シンボルレート** | **374.4 ksps** |
| **ロールオフ率 α** | **0.2（送受均等配分＝RRC 各 0.2）** |
| 伝送モード | 音声リニア PCM／コンポジットリニア PCM |
| サンプリング | 48 kHz（音声）／120 kHz（コンポジット） |
| 量子化 | 16 bit |
| 音声特性 | 20 Hz–20 kHz、±0.5 dB、S/N ≥70 dB、歪 ≤0.15% |

**導出**: B99 ≈ R_s(1+α) の 99% 占有 ≈ 374.4k × 1.08 ≈ **405 kHz** ✓（α=0.2 と整合）。
64QAM/RRC α=0.2 の **PAPR ≈ 6〜6.5 dB**（0.01% CCDF）。

## 3. 受信 RF

| 項目 | 値 |
|---|---|
| NF | ≤ 4 dB |
| 受信周波数 | 6716.625 MHz、引込 ±7 kHz |
| 入力レベル範囲 | −30 〜 −80 dBm、標準 −65.5 dBm、復調可能 ≤−80 dBm |
| RX-LO | 5719.625 MHz |

## 4. TX 切替装置（DSTS-6500SR）

| 項目 | 値 |
|---|---|
| 入力周波数 | M: **6570–6870 MHz** / N: 7455–7750 MHz |
| RF 入力 | **+30 dBm typ.**（＝送信機 1 W） |
| RF 出力 | **+28 dBm typ.**（切替損 ~2 dB）、50 Ω |

## 5. その他

- 消費電力: TX 150 W／RX 50 W／PS 300 W（運用時）。動作温度 0–40℃。
- 電源: TX DC24V、RX/PS AC100V。筐体 429×88.5×430（1U/2U）。
- N バンド: 7425–7750 MHz（将来）。

---

## 6. 本プロジェクトへの反映（確定値）

| これまでの仮定 | 実機の確定値 |
|---|---|
| 出力 ≤2 W（規制上限） | **従来実機は 1 W(+30 dBm)**。2 W は規制上限で、増力するなら外付け PA |
| PAPR 5〜7 dB（仮） | **α=0.2 → PAPR ≈ 6〜6.5 dB** |
| 占有帯 ≤405 kHz | **確定**（R_s=374.4 ksps, α=0.2） |
| マスクは例示 | **実マスク: −37 dBc@±250 kHz / −48 dBc@±750 kHz** |
| 周波数 6.5 GHz 帯 | **6716.625 MHz（MAF-33）** |

**電力バジェット（実機 1 W 基準）**:
- 平均 +30 dBm、PAPR ~6.5 dB → **尖頭 ~+36.5 dBm（≈4.5 W）**。
- 線形（64QAM, マスク −37/−48 dBc）を満たすには尖頭が Psat より数 dB 下 → **デバイス Psat ~+40 dBm(10W) 級**が安全（DPD で 6〜8 W 級も可）。
- ⇒ Qorvo QPA1013D(10W) / Wolfspeed CMPA601C025F(25W) は **1 W 設計でも 2 W 増力でも適合**。

**プロジェクトの目的（確定）**:
- **原設計(2020)の 1 W PA は GaAs デバイス。その GaAs が廃番（EOL・入手不可）→ GaN で作り直す**のが本件の目的。
- したがって目標は **増力ではなく「1 W 仕様（+30 dBm・マスク −37/−48 dBc・占有 ≤405 kHz）を現行 GaN で再現」**する EOL 置換。
- **DPD の役割 = GaAs と GaN の線形性差を埋める保険**。GaAs は素で線形性が良く旧機は DPD 無しでマスクを満たせた可能性が高い。GaN は同バックオフだと線形性がやや劣るので、届かない分を DPD で補う。
- 狭帯域(405 kHz)・1 W ゆえ、GaN を十分バックオフすれば DPD 無しで届く公算あり。**まず DPD 無しで実測 → マスク余裕を確認 → 足りなければ MP-DPD**。
- 推奨デバイス: **Qorvo QPA1013D（GaN MMIC, 6–18 GHz, 10 W, 現行品）**。1 W へ ~9 dB バックオフで高線形、GaAs 代替に好適。
